import 'package:flutter/material.dart';

/// Basic colors common to this application
class ColorList {
  /// Basic colors common to this application
  static const pink = Color(0xFFF02D69);

  /// voice call button
  static const lightRed = Color(0xFFEC595E);

  /// Basic colors common to this application
  static const orange = Color(0xFFFF8412);

  /// Basic colors common to this application
  static const blue = Color(0xFF0179D5);

  /// Card, horizontal line Background color of
  static const paleGrey = Color(0xFFF9F8F8);

  /// Thin text color
  /// Unselected Tab text color
  static const lightGrey = Color(0x50000000);

  /// Grey Dark card paymentCard
  static const grey = Color(0xFFEEEEEE);

  /// Grey Accent color
  static const greyAccentColor = Color(0xFF979797);

  /// Color of text characters other than black
  static const darkGrey = Color(0xFF4E4E4E);

  /// Color of text form field's hint texts.
  static const hintTextColor = Color(0xFF898A8D);

  /// Color of text form field's text button.
  static const hintTextButton = Color(0xFFD9D9D9);

  /// Color of radio label text.
  static const radioLabel = Color(0xFF707070);

  /// Color of text in the Account Screen.
  static const white = Color(0xFFFFFFFF);

  /// Black color for tab titles
  static const black = Color(0xFF000000);

  /// Color of text of the title of a page
  static const pageTitleColor = Color(0xFF404040);

  /// Income List item bottom border color
  static const borderBottomColor = Color(0xFFD6D6D6);
  
  /// Delivery details screen list item border color
  static const deliveryDetailsItemBorderColor = Color(0xFF898A8D);

  /// Driver Info. Reg. screen info text color
  static const driverInfoRegInfoTextColor = Color(0xFF707070);

  /// Delivery details daily screen title color
  static const deliveryDetailsDailyTitleColor = Color(0xFF414141);


  /// home pending list unselected tab text color color
  static const unselectedTabText = Color(0xFFCCCCCC);

  /// home pending list tabManBorder color
  static const tabManBorder = Color(0xFF7AB92D);

  /// processing list card color
  static const processingCard = Color(0xFFF3F3F3);

  /// processing list details text color
  static const processingListDetailsTextColor = Color(0xFF181818);

  /// processing list image upload border color
  static const processingListImageUploadBorderColor = Color(0xFF9B9B9B);



  /// Bicycle Info. Reg. screen text color
  static const bicycleInfoRegTextColor = Color(0xFF606060);

  /// Bicycle Info. Reg. page indicator color
  static const bicycleInfoRegPageIndicatorColor = Color(0xFFB2B2B2);

  /// Image Upload mask color
  static const imageUploadMaskColor = Color(0xFFE2F0FF);

  /// Registration of information on light vehicles Registration completed Screen color
  static const lightVehicleRegInfoBackground = Color(0xFFF1F1F1);

  /// Registration Completed vehicle information card background color
  static const registeredVehicleCardBackgroundColor = Color(0xFFEAEAEA);

  ///Password Reset hint color
 static const lightHintGrey = Color(0xffc0c0c1);

  /// Account Screen user active status color
  static const userActiveStatusColor = Color(0xFFA0ED23);

  /// Online Screen order data fetched container color
  static const orderFetchedContainerColor = Color(0xFFCAD8ED);

  static const passwordResetTextFieldHintTextColor = Color(0xFF898A8D);


}
